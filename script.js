function pcChoice() {
  let list = ["rock", "paper", "scissors"];
  return list[Math.random() * list.length];
}

function myChoice() {
  let choice = prompt("Dute fanpulamea alege").toLowerCase();
  return choice;
}

human;

function playRound(myChoice, pcChoice) {}
